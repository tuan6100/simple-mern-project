services:
  mongodb:
    image: mongo:latest
    container_name: student-mongo
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=student_db
    volumes:
      - ./mongodbdata:/data/db
